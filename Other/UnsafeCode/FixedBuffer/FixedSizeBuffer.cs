using System;

namespace LearnPointer
{
    class FixedSizeBuffer
    {
        static unsafe void Main()
        {
            Console.WriteLine("Size of structure:" + sizeof(FixedBankRecord));
        }
    }
}
